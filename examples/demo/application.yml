spring:
  application:
    name: CUSTOM_EASE_GATEWAY_TEST
  profiles:
    active: dev,metrics,skylb

# gRPC
grpc:
  port: 9090
skylb:
  address: skylb://localhost:1900/

# metrics
metrics:
  enabled: false
  scrapePort: 10080

# ----------------------------------------
# CORE PROPERTIES
# ----------------------------------------
# Enable debug logs.
debug: false
# Enable trace logs.
trace: false
